import Default from '../layout/Default';

const page = 'Appearance';
const pageTitle = 'Appearance';
const pageDescription = `Manage your Account Appearance.`;

function Appearance() {
  return (
    <Default
      page={page}
      pageTitle={pageTitle}
      pageDescription={pageDescription}
    >
      Appearance
    </Default>
  );
}

export default Appearance;
